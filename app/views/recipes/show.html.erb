<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @recipe.name %>
</p>

<p>
  <strong>User:</strong>
  <%= @recipe.user_id %>
</p>

<table>
  <thead>
    <tr>
      <th>Contents</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @recipe_foods.each do |recipe_food| %>
      <tr>
        <td><%= recipe_food.contents %></td>
        <td><%= link_to 'Show', recipe_food %></td>
        <td><%= link_to 'Edit', edit_recipe_food_path(recipe_food) %></td>
        <td><%= link_to 'Destroy', recipe_food, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= form_with model: @recipe_food do |form| %>
  <% if @recipe_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe_food.errors.count, "error") %> prohibited this recipe_food from being saved:</h2>

      <ul>
        <% @recipe_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contents %>
    <%= form.text_field :contents %>
  </div>

  <%= form.hidden_field :recipe_id, value:@recipe.id %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<table>
  <thead>
    <tr>
      <th>Step</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @recipe_processes.each do |recipe_process| %>
      <tr>
        <td><%= recipe_process.step %></td>
        <td><%= link_to 'Show', recipe_process %></td>
        <td><%= link_to 'Edit', edit_recipe_process_path(recipe_process) %></td>
        <td><%= link_to 'Destroy', recipe_process, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= form_with model: @recipe_process do |form| %>
  <% if @recipe_process.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe_process.errors.count, "error") %> prohibited this recipe_process from being saved:</h2>

      <ul>
        <% @recipe_process.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :step %>
    <%= form.text_field :step %>
  </div>

  <%= form.hidden_field :recipe_id, value:@recipe.id %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>



<%= link_to 'Edit', edit_recipe_path(@recipe) %> |
<%= link_to 'Back', recipes_path %>
